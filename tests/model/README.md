# Tests Model

Ce dossier contient les tests des modèles de base de données utilisés dans le projet.

- **test_db.py** : Vérifie la création, l'ajout et la manipulation des objets de la base de données (Server, Entrypoint, OasisNode, etc.).

Lancez les tests avec `pytest tests/model/`.
