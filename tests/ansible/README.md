# Tests Ansible

Ce dossier contient les tests d'intégration avec Ansible.

- **test_ansible.py** : Vérifie l'exécution d'un playbook Ansible via le code Python.

Lancez les tests avec `pytest tests/ansible/`.
