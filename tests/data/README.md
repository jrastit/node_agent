# Tests Data

Ce dossier contient les tests liés à la gestion et la transformation des données de configuration Oasis.

- **test_oasis_config_data.py** : Vérifie l'ajout et la récupération des données de configuration à partir de fichiers markdown.

Lancez les tests avec `pytest tests/data/`.
